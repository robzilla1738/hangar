# Project skills to invoke from phase goals

- **macos** — for SwiftUI + AppKit + macOS-26 (Liquid Glass) patterns; invoke during phases that touch chrome, window mgmt, menus, native panels
- **swift** — Swift 6 strict concurrency review during foundation + core phases
- **design** — visual polish phase (Liquid Glass surfaces, motion, vibrancy)
- **security** — API key storage (Keychain), notarization, hardened runtime entitlements
- **testing** — XCTest setup, characterization tests for agent profile parsing
- **product** — architecture spec doc generation early in the run
- **release-review** — final phase, pre-tag senior-dev review

Each phase spec should reference the skill(s) to consult in its "Approach" section.
