# Screenshots

Marketing-quality screenshots committed here. Capture commands and the
state setup live in `scripts/capture-screenshots.sh` (lands in a
sub-phase).

| File | Purpose |
|---|---|
| `hero.png` | Main window with 3 panes (Claude + Codex + shell), cost pill, 2 approvals in inbox |
| `mission-control.png` | Cmd-0 overlay with 4+ tiles, one needing attention |
| `approval-inbox.png` | Inbox popover with two pending approval items |
| `worktrees.png` | Sidebar shelf with 3 worktrees, one dirty |
| `diff-sidecar.png` | Right sidecar with two file diffs visible |
| `theme-comparison.png` | Side-by-side Hangar Dark vs Hangar Light |

**Status**: placeholder. Live captures land once Cmd-0 and the inbox UI
are wired through Phase 6/7 sub-phases. README hero image will inline
`hero.png` once present.
